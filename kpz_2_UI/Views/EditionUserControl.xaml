<UserControl x:Class="kpz_2_UI.Views.EditionUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:kpz_2_UI.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <DataGrid ItemsSource="{Binding Path=Editions}" 
                  AutoGenerateColumns="True" Height="348" 
                  CanUserAddRows="True"  HorizontalAlignment="Left" 
                  Name="dataGrid1" VerticalAlignment="Top" Width="480" 
                  SelectedItem="{Binding Path=SelectedOrder, Mode=TwoWay}"
                  Margin="10,65,0,0" Background="#FFDCF4F4" BorderBrush="#FF2E98FF" FontFamily="Times New Roman"
                  FontSize="14"
                  
                  />

        <Label Content="Editions" HorizontalAlignment="Left" Margin="10,10,0,0" VerticalAlignment="Top" FontSize="20" FontFamily="Georgia"/>


    </Grid>
</UserControl>
